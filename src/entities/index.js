const { Vehicle, NullVehicle } = require('./vehicle')
const { Unit, NullUnit } = require('./unit')
const {
	LicensePlateDetails,
	NullLincesePlateDetails,
} = require('./licencePlate')
const { Permission } = require('./permission')

module.exports = {
	Vehicle,
	NullVehicle,
	Unit,
	NullUnit,
	LicensePlateDetails,
	NullLincesePlateDetails,
	Permission,
}
